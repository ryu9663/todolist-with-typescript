import React from "react";
import styled from "styled-components";

const TitleWrapper = styled.div`
  font-size: 2rem;
  text-align: center;
`;
function Title() {
  return <TitleWrapper>투두리스트</TitleWrapper>;
}

export default Title;
